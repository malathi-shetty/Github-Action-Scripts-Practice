name: Testing Workflow

on: push

jobs:
    test-job:
        runs-on: ubuntu-latest

        steps:
            - name: Hello display
              run: echo "Hello KK FUNDA"

            - name: Display the date
              run: date